## getting start 

- `uv --version` : dependency check   
- `uv sync` : dependency sync   
- `uv add  ipykernel jedi` : install dependency package.  
- `uv run python xxx.py` or `source .venv/bin/activate`  
- `deactivate` : exit venv environment 
